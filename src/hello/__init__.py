# Comment out the following if we want to expose everything to the package level.
from hello.age import *     # same as 'from .age import *'
from hello.name import *    # same as 'from .name import *'
